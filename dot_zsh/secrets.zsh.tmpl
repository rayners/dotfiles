# This file is managed by chezmoi
# It sources your personal secrets file that is not tracked in the repository

# Source the external secrets file if it exists
if [[ -f "${HOME}/.secrets.zsh" ]]; then
  source "${HOME}/.secrets.zsh"
fi

# You can add placeholder functions or defaults here that will work
# even if the secrets file doesn't exist

# Example:
# if ! type some_secret_function > /dev/null; then
#   some_secret_function() {
#     echo "Warning: Using placeholder function. Set up your secrets file."
#   }
# fi